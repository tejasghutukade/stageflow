id: playwright-mcp
stages:
  - id: screenshot
    uses: ./screenshot.yaml
    mcp: [playwright]
    completion:
      mode: all
      checks:
        - id: png
          type: artifact
          path: page.png
          nonempty: true
