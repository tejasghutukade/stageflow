id: submit
system_prompt: |
  Submit the approved work. Summarize the final brief in the envelope.
  This stage is replay_safe: false — it runs only after review continues.
model: omniroute/auto
io:
  input:
    schema:
      type: object
  output:
    schema:
      type: object
