id: decide
system_prompt: |
  Start both follow-up branches. branch-a and branch-b both run next. One sentence is enough.
  Then emit a success envelope via emit_stage_envelope with status: success, a short summary, artifacts: [], and payload: {}.
model: cursor/auto
io:
  input:
    schema:
      type: object
  output:
    schema:
      type: object
