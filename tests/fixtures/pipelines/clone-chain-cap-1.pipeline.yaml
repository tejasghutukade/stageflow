id: clone-chain-cap-1
schemas:
  Issue:
    type: object
    required: [id, title]
    properties:
      id:
        type: string
      title:
        type: string
stages:
  - id: emit-items
    entry: true
    clone_cap: 1
    clone_mode: parallel
    route:
      - to: handle-item
    system_prompt: Emit a list of issues.
    model: test/model
    io:
      input:
        schema:
          type: object
      output:
        schema:
          type: object
          required: [items]
          properties:
            items:
              type: array
              items:
                $ref: "#/schemas/Issue"
            summary:
              type: string
  - id: handle-item
    route:
      - to: gather
    system_prompt: Handle one issue.
    model: test/model
    io:
      input:
        schema:
          $ref: "#/schemas/Issue"
      output:
        schema:
          type: object
          required: [id]
          properties:
            id:
              type: string
  - id: gather
    system_prompt: Gather clone results.
    model: test/model
    io:
      input:
        schema:
          type: object
      output:
        schema:
          type: object
