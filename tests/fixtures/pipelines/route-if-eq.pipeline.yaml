id: route-if-eq
stages:
  - id: triage
    entry: true
    route:
      - to: page
        if:
          field: severity
          op: eq
          value: high
      - to: notify
    system_prompt: Triage the payload.
    model: test/model
    io:
      input:
        schema:
          type: object
      output:
        schema:
          type: object
          required: [severity]
          properties:
            severity:
              type: string
  - id: page
    system_prompt: Page on-call.
    model: test/model
    io:
      input:
        schema:
          type: object
      output:
        schema:
          type: object
  - id: notify
    system_prompt: Notify the team.
    model: test/model
    io:
      input:
        schema:
          type: object
      output:
        schema:
          type: object
