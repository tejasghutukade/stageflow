include:
  - local: ./a.pipeline.yaml
stages:
  - id: nested
    system_prompt: Nested stage.
    model: cursor/auto
    io:
      input:
        schema:
          type: object
      output:
        schema:
          type: object
